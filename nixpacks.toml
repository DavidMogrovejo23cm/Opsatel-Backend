[phases.setup]
nixPkgs = ["chromium", "nodejs", "python311", "gcc"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install --omit=dev"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
